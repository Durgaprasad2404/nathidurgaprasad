const projectElement = document.getElementById("projcetBtn");
const projectContainer = document.getElementById("projects");

projectElement.addEventListener("click", function () {
  projectContainer.style.display = "flex";
});
